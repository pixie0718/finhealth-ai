# Copy to .env to point the frontend at a non-local backend. .env is gitignored.
# Create React App only exposes vars prefixed with REACT_APP_ (read at build time).
REACT_APP_API_URL=http://localhost:8000
